You are cheap BUILDER. Return ONLY valid JSON {files:[{path,content}]} max 8000 output tokens. Complete Unity 6000.6 URP InputSystem1.20 local core loop game source. Paths under kwaffee/Assets/KwaFee/Runtime only. Namespace KwaFee. Files: KwaFee.Runtime.asmdef (Unity.InputSystem ref); FlingRules.cs pure static charge speed and tips; CoffeeCup.cs Rigidbody MonoBehaviour; Barista.cs Rigidbody MonoBehaviour; ServeZone.cs trigger MonoBehaviour; CoreGame.cs composition/input/UI/physics tick. Each class filename matches. No editor scripts, external APIs, downloads, generated primitives, stubs, automatic editor execution or runtime LLM.

All 3D art comes from assigned public GameObject fields cupPrefab,baristaPrefab,customerPrefab,counterPrefab,floorPrefab,wallPrefab,trayPrefab,signPrefab on CoreGame. Prefabs have MeshFilter/Renderer from Blender. Dimensions Cup .4high, Barista/Customer1.8high, Counter3x1x1, Floor16x12 flat top y0, Wall16wide .25deep4high, Tray1.4x.8x.1. Separate editor tool sets prefabs and camera. Runtime CoreGame.Start initializes actual gameplay. Public Initialize(int seed,bool botMode) creates same world, public Step(float dt) called FixedUpdate (1/30), public Tick and counters Shots,Serves,Misses,Catches,Hits,TotalTips, public Players and Cups collections. Instance state only, no singleton. Camera (0,12,-12) looking(0,0,1). Customer (0,0,4.6), serve tray (0,1,3.5), rack counter(-4,0,0), E at rack spawns filled cup from 16 cup pool. Four baristas x -3,-1,1,3 z-3, first human controlled, others hittable. Bounds walls. Public command interface on Barista: Move(Vector2), Aim(Vector3), TryGrab(), Drop(), BeginCharge(), ReleaseCharge(). Same commands for sim.

Cups state Pool/Rack/Held/Airborne/Served; real Rigidbody and collider. Liquid spills from impact or tipping; valid serve requires actual airborne cup entry into serve trigger, liquid>=.2, valid owner id, one award per cup. Recycle after serve delay/miss age/outside bounds. Reset ownership/timers/transform/velocity/liquid/collision flags. Grab closest within1.2m only when hands empty, excludes served/pool/another held; rack replenishes with pool. Held physics kinematic follows hand point in Step without own-body collision; reenable when thrown. Reachable monotonic charge from speed5 to12 over1.1s, launch upward component .5, angular spin. CapsuleCollider or box approximation. Cup hitting barista at speed>=3 gives .65s stun and spill, owner grace .15s only. Keep shot counters actual. Instantiation only at initialization/pool setup, no per-frame searches/LINQ or allocations except HUD text. Visual squash/bobble only on mesh child.

CoreGame.Step updates charge/stun/cup ages and movement; normal play automatic physics, sim can explicitly Step and PhysicsScene.Simulate. InputSystem Keyboard/Mouse in Update, camera ray intersect ground for aim, WASD/E/R/clickhold; button transitions once. OnGUI candy-colored chunky HUD using default font: KWA FEE, one-line controls, charge bar, serve count/tips, order, contextual rack/grab prompt, event-driven Boston roast. Own Boston lines, no praise. No other verbs/audio/netcode until later artifacts. Floor and wall colliders attached to authored meshes, collider-only empty objects allowed. Missing prefabs actionable error, never invisible or primitive substitute. No code automatically runs outside Play Mode. You cannot run Unity here; no fabricated tests or metrics. Source under 550 lines total, complete code only in JSON response.
